extends layout

block content
    h1 #{title}
    ul.list-group
        each blogpost, i in blogposts
            //- '=' writes the variable
            li.list-group-item
                a(href="/blogpost/"+blogpost._id)= blogpost.title